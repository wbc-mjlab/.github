# Organization project page (Eleventy)

Minimal academic one-pager (title · links · teaser · abstract · embedded demo · code · BibTeX).

Published: **https://wbc-mjlab.github.io/.github/**

```bash
npm install
npm start      # http://localhost:8080/.github/
npm run build
```

Edit `src/_data/site.json` / `src/index.njk`. Media from `../profile/assets/`.
